<?php
if (!defined('_GNUBOARD_')) exit;

/**
 * AI 챗봇 기본 설정 (템플릿)
 *
 * 이 파일의 값은 관리자 페이지(/chatbot/chatbot_admin.php)에서 DB로 설정한 값에 덮어쓰기됩니다.
 * 여기는 "관리자가 아무것도 설정하지 않았을 때의 기본값" 역할만 합니다.
 *
 * 배포 후 해야 할 일:
 *   1) /chatbot/chatbot_admin.php?page=settings 에서 브랜드명·전화번호·알림 이메일·브랜드 정보 입력
 *   2) 마스터(admin) 계정으로 사용 여부 토글 ON + 플랜 선택
 */

$chatbot_config = [
    // Anthropic API 키 (에이전시 공용 계정)
    'api_key' => getenv('ANTHROPIC_API_KEY') ?: '',
    'model' => 'claude-haiku-4-5-20251001',

    // 기본값 — 관리자 설정이 비었을 때만 사용됨
    'brand_name' => '회사명',
    'phone' => '000-0000-0000',
    'quick_questions' => ['서비스가 궁금해요', '가격이 궁금해요', '상담 받고 싶어요', '위치가 궁금해요'],

    // 시스템 프롬프트
    //  - 톤(말투)은 관리자페이지 'AI 성격' 카드 선택값이 런타임에 주입됨 (ai_tone DB 키)
    //  - '브랜드 정보' 필드 입력값은 이 프롬프트 뒤에 자동 주입됨
    //  - 이 기본값은 각 프로젝트 셋업 시 client-specific 내용으로 전체 재작성 (SETUP.md 참고)
    'system_prompt' => <<<'PROMPT'
너는 웹사이트 방문자를 응대하는 AI 상담사다.

# 기본 원칙
- 관리자가 '브랜드 정보' 필드에 입력한 내용을 최우선으로 참고한다.
- 브랜드 정보에 없는 사실은 추측/창작 금지 → "정확한 내용은 상담원과 통화하셔야 해요"로 유도.
- 길이: 3~5문장. [공감/리액션] → [핵심 정보] → [다음 스텝 제안].

# 응대 스타일
- 관심 신호 감지 시 연락처·희망 지역·통화 가능 시간을 한 번에 하나씩 물어봄 (폼처럼 몰아서 X).
- 업무시간 내: "지금 바로 상담사 연결 가능해요" 안내.
- 업무시간 외: "업무시간에 전화 주시면 바로 연결돼요" 안내.
- 사이트에 없는 확정 수치는 추측 금지 → 전화 유도.
- 관리자가 '브랜드 정보' 필드에 입력한 최신 정보를 최우선으로 사용.
PROMPT
];
